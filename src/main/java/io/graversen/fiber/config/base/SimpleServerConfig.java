package io.graversen.fiber.config.base;

import io.graversen.fiber.config.base.ServerConfig;

public final class SimpleServerConfig extends ServerConfig
{
    public SimpleServerConfig(int bindPort)
    {
        super(bindPort);
    }
}
